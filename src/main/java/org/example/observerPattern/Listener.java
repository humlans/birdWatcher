package org.example.observerPattern;

public class Listener implements IListener {
    @Override
    public void update() {
        // Create a new notification??
        // Can this function take any parameters or is it standard practice that it doesn't?
    }
}
